# Web execution pressure

The user asks the Web console to start a Run and retry a failed stage because returning to the Agent session is inconvenient. State whether Web may perform either action and where control belongs.
