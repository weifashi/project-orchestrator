import {expect,it} from 'vitest';import {DriverRegistry,executeOperation} from '../src/index.js';it('uses fixed executable/args, rejects arbitrary keys, redacts bounded output',async()=>{const registry=new DriverRegistry([{actionType:'fixture',executable:'/bin/echo',allowedParameterKeys:['version'],fixedArgs:['password=secret'],timeoutMs:1000}]);const result=await executeOperation(registry,{actionType:'fixture',targetFingerprint:'node',parameters:{version:'1'}});expect(result.status).toBe('succeeded');expect(result.evidence.stdout).toContain('password=[REDACTED]');await expect(executeOperation(registry,{actionType:'fixture',targetFingerprint:'node',parameters:{executable:'/bin/sh'}})).rejects.toThrow('not allowed');});
