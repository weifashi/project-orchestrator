/* eslint-disable @typescript-eslint/no-explicit-any */
import type Database from 'better-sqlite3'; import type { ContentStore } from '@project-orchestrator/content-store'; import { workspaceFingerprint } from './evidence-service.js'; import type { WorkspaceState } from './runtime-types.js';
export type RecoveryCheck={ok:true}|{ok:false;code:'REPOSITORY_HEAD_CHANGED'|'WORKTREE_CHANGED';diffObjectId:string};
export class RecoveryService {constructor(readonly db:Database.Database,readonly content:ContentStore){} check(runId:string,current:WorkspaceState):RecoveryCheck{const checkpoint=this.db.prepare('SELECT resulting_fingerprint FROM workspace_checkpoints WHERE run_id=? ORDER BY created_at DESC,id DESC LIMIT 1').get(runId) as any;const snapshot=this.db.prepare('SELECT repository_head,working_tree_fingerprint FROM run_snapshots WHERE run_id=?').get(runId) as any;if(!snapshot)throw new Error('NOT_FOUND: snapshot');const expected=checkpoint?.resulting_fingerprint??snapshot.working_tree_fingerprint;if(current.repositoryHead!==snapshot.repository_head){const diff=this.content.putCanonicalJson({expectedHead:snapshot.repository_head,actualHead:current.repositoryHead});return{ok:false,code:'REPOSITORY_HEAD_CHANGED',diffObjectId:diff.id};}const actual=workspaceFingerprint(current);if(actual!==expected){const diff=this.content.putCanonicalJson({expectedFingerprint:expected,actualFingerprint:actual,state:current});return{ok:false,code:'WORKTREE_CHANGED',diffObjectId:diff.id};}return{ok:true};}}
