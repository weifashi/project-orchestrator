import {describe,expect,it} from 'vitest';import {validateWorkflowGraph} from '../src/index.js';
const stage=(key:string,extra:Record<string,unknown>={})=>({key,role_version_id:'role',optional:false,mandatory_gate:false,failure_policy:'fail' as const,max_attempts:1,requires_confirmation:false,...extra});
describe('graph validation',()=>{it('uses stable Kahn order and rejects cycles/unreachable roots',()=>{const workflow={slug:'x',version:1,stages:[stage('root'),stage('b'),stage('a')],edges:[{from:'root',to:'a',edge_type:'requires' as const},{from:'root',to:'b',edge_type:'requires' as const}],iteration_groups:[]};expect(validateWorkflowGraph(workflow)).toEqual(['root','a','b']);expect(()=>validateWorkflowGraph({...workflow,edges:[...workflow.edges,{from:'a',to:'root',edge_type:'requires' as const}]})).toThrow('cycle');expect(()=>validateWorkflowGraph({...workflow,edges:[]})).toThrow('exactly one root');});it('rejects bypassable gates and more than three iterations',()=>{const workflow={slug:'x',version:1,stages:[stage('root',{iteration_group_key:'g'}),stage('gate',{iteration_group_key:'g',mandatory_gate:true,optional:true})],edges:[{from:'root',to:'gate',edge_type:'requires' as const}],iteration_groups:[{key:'g',entry_stage_key:'root',gate_stage_keys:['gate'],aggregation_policy:'collect_all' as const,max_iterations:4}]};expect(()=>validateWorkflowGraph(workflow)).toThrow('max_iterations');});});
