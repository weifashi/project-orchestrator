import {expect,it} from 'vitest';import {reduceIteration} from '../src/index.js';const group={key:'delivery',entry_stage_key:'implementation',gate_stage_keys:['review','test'],aggregation_policy:'collect_all' as const,max_iterations:3};it('bounds iterations and never reopens history',()=>{expect(reduceIteration(group,{iterationNumber:2,status:'running'},{review:'failed',test:'succeeded'})).toMatchObject({createIteration:3,createStageRuns:['implementation','review','test'],markIteration:'failed'});expect(reduceIteration(group,{iterationNumber:3,status:'running'},{review:'failed',test:'succeeded'})).toEqual({createStageRuns:[],markIteration:'failed',markRunFailed:true});expect(reduceIteration(group,{iterationNumber:3,status:'running'},{review:'succeeded',test:'succeeded'})).toEqual({createStageRuns:[],markIteration:'succeeded',markRunFailed:false});});
