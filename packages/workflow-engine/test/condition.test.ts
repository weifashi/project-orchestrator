import {describe,expect,it} from 'vitest';import {evaluateCondition} from '../src/index.js';const context={input:{enabled:true,kind:'feature'},outputs:{test:{ok:true}},constants:{limit:3}};
describe('condition DSL',()=>{it('supports finite operators and roots',()=>{expect(evaluateCondition({op:'all',items:[{op:'eq',path:'/input/enabled',value:true},{op:'in',path:'/input/kind',values:['bug','feature']},{op:'not',item:{op:'ne',path:'/constants/limit',value:3}},{op:'exists',path:'/outputs/test/ok'}]},context)).toBe(true);expect(evaluateCondition({op:'any',items:[{op:'eq',path:'/input/enabled',value:false},{op:'eq',path:'/input/kind',value:'feature'}]},context)).toBe(true);});it('fails closed on missing or illegal paths',()=>{expect(()=>evaluateCondition({op:'exists',path:'/input/missing'},context)).toThrow('CONDITION_EVALUATION_FAILED');expect(()=>evaluateCondition({op:'eq',path:'/env/HOME',value:'x'},context)).toThrow('CONDITION_EVALUATION_FAILED');});});
